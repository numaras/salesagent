/**
 * Background job: poll active media buy statuses.
 * Stub — will be fleshed out once adapter sync logic is ready.
 */

export async function pollMediaBuyStatus(): Promise<void> {
  console.log("Polling media buy status...");
}
