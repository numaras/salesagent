// TODO: Integrate with creative agent property discovery once adapter supports it.

export async function discoverProperties(
  _tenantId: string
): Promise<{ discovered: number }> {
  return { discovered: 0 };
}
